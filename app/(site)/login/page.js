import PageContent from '@/lib/PageContent';

export const metadata = {
  title: 'Login — BGSCET | MBA Portal',
  description: 'Student and Faculty login portal for BGSCET MBA, Bengaluru.',
};

export default function LoginPage() {
  return <PageContent name="login" />;
}
