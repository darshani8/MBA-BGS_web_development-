import PageContent from '@/lib/PageContent';

export const metadata = {
  title: 'Campus Gallery | BGSCET MBA Bengaluru',
  description:
    'Explore life at BGSCET MBA — campus, cultural fests, library, guest lectures, sports and convocation. Filter the gallery by category.',
};

export default function GalleryPage() {
  return <PageContent name="gallery" />;
}
